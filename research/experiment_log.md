# Experiment log

Record dated runs, exact configuration hashes, model/checkpoint identities, qualitative failures, and
measured validation results here. Never include patient identifiers or screenshots in Git.

