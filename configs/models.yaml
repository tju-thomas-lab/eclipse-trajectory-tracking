version: "0.1.0"
backends:
  deterministic:
    enabled: true
    description: "Reports measured visual state changes without guessing GUI actions."
  local_openai_compatible:
    enabled: false
    endpoint: "http://127.0.0.1:8000/v1"
    model: null
    localhost_only: true
    description: "Optional local multimodal server (for example vLLM or llama.cpp)."

